# Mac版 Excel ショートカットキー一覧（US キーボード対応）

使用頻度が高い順に分類して掲載しています。
キー表記は **US 配列**基準です。JIS 配列では一部キーの位置が異なります。

---

## 1. 超頻出（毎日何十回も使う）

| ショートカット | 操作 |
|---|---|
| `⌘ + C` | コピー |
| `⌘ + V` | 貼り付け |
| `⌘ + X` | 切り取り |
| `⌘ + Z` | 元に戻す（Undo） |
| `⌘ + Y` / `⌘ + Shift + Z` | やり直し（Redo） |
| `⌘ + S` | 上書き保存 |
| `⌘ + A` | 全選択 |
| `Enter` | 入力確定（下に移動） |
| `Tab` | 入力確定（右に移動） |
| `Esc` | 入力キャンセル |
| `Delete` / `fn + Delete` | セル内容を削除 |

---

## 2. セル移動・選択（非常によく使う）

| ショートカット | 操作 |
|---|---|
| `⌘ + ↑↓←→` | データの端まで一気にジャンプ |
| `⌘ + Shift + ↑↓←→` | データの端まで一気に選択 |
| `Shift + ↑↓←→` | 選択範囲を1セルずつ拡張 |
| `⌘ + Home`（`⌘ + fn + ←`） | A1 セルに移動 |
| `⌘ + End`（`⌘ + fn + →`） | データ末尾セルに移動 |
| `Ctrl + G` / `fn + F5` | ジャンプ（指定セルへ移動） |
| `⌘ + F` | 検索 |
| `⌘ + H` | 置換 |

---

## 3. 書式設定（頻繁に使う）

| ショートカット | 操作 |
|---|---|
| `⌘ + B` | 太字 |
| `⌘ + I` | 斜体（イタリック） |
| `⌘ + U` | 下線 |
| `⌘ + 1` | セルの書式設定ダイアログを開く |
| `Ctrl + Shift + ~`（`` ` `` キー＝`1` の左） | 標準の表示形式 |
| `Ctrl + Shift + !` | 桁区切り（カンマ区切り） |
| `Ctrl + Shift + $` | 通貨の表示形式 |
| `Ctrl + Shift + %` | パーセントの表示形式 |
| `⌘ + Shift + >` | フォントサイズを大きく |
| `⌘ + Shift + <` | フォントサイズを小さく |

---

## 4. 行・列・セルの操作（よく使う）

| ショートカット | 操作 |
|---|---|
| `Ctrl + -` | セル／行／列の削除 |
| `Ctrl + Shift + +` | セル／行／列の挿入 |
| `⌘ + Shift + K` | 行全体を選択してから削除（行削除） |
| `Shift + Space` | 行全体を選択 |
| `Ctrl + Space` | 列全体を選択 ※下記注意参照 |
| `⌘ + D` | 上のセルをコピーして貼り付け（フィルダウン） |
| `⌘ + R` | 左のセルをコピーして貼り付け（フィルライト） |
| `⌘ + Shift + V` | 形式を選択して貼り付け |

> **注意**: `Ctrl + Space` は macOS の「入力ソース切替」と競合することがあります。
> 「システム設定 → キーボード → キーボードショートカット → 入力ソース」で macOS 側のキーを変更するか無効にしてください。

---

## 5. 数式・関数（実務で必須）

| ショートカット | 操作 |
|---|---|
| `=` | 数式入力の開始 |
| `fn + F4`（数式バー編集中） | 参照の切替（相対 → 絶対 → 複合） |
| `⌘ + Shift + Enter` | 配列数式として確定（旧バージョン） |
| `fn + F9` | 数式バー内で選択部分を計算結果に変換（確認用） |
| `` Ctrl + ` ``（`1` の左のキー） | 数式の表示／値の表示を切替 |
| `⌘ + Shift + A` | 関数の引数名を表示（数式入力中） |
| `Shift + fn + F3` | 関数の挿入ダイアログ |

---

## 6. シート操作

| ショートカット | 操作 |
|---|---|
| `Option + →` / `Option + ←` | 次のシート／前のシートに移動 |
| `Shift + fn + F11` | 新しいシートを挿入 |
| `⌘ + Option + →` / `⌘ + Option + ←` | 次／前のブック（ウィンドウ）に切替 |

---

## 7. テーブル・フィルタ・並べ替え

| ショートカット | 操作 |
|---|---|
| `⌘ + T` | テーブルとして書式設定（セル選択時。数式バー内では参照切替になる） |
| `⌘ + Shift + L` | オートフィルタの ON／OFF |
| `⌘ + Shift + R` | フィルタメニューを表示（フィルタ矢印にフォーカス時） |

---

## 8. 印刷・ファイル操作

| ショートカット | 操作 |
|---|---|
| `⌘ + P` | 印刷 |
| `⌘ + N` | 新規ブック |
| `⌘ + O` | ファイルを開く |
| `⌘ + W` | ブックを閉じる |
| `⌘ + Q` | Excel を終了 |
| `⌘ + Shift + S` | 名前を付けて保存 |

---

## 9. その他の便利なショートカット

| ショートカット | 操作 |
|---|---|
| `Ctrl + ;`（セミコロン） | 今日の日付を入力 |
| `Ctrl + Shift + ;` | 現在の時刻を入力 |
| `Ctrl + Option + V` | 形式を選択して貼り付け（詳細ダイアログ） |
| `fn + F7` | スペルチェック |
| `fn + F2` | セルの編集モードに切替（カーソルがセル内に入る） |
| `⌘ + K` | ハイパーリンクの挿入 |
| `Option + Enter` | セル内で改行 |

---

## 10. ファンクションキー早見表

Mac では `fn` キーを押しながらファンクションキーを使います（システム環境設定で変更可）。

| キー | 操作 |
|---|---|
| `fn + F1` | ヘルプ |
| `fn + F2` | セルの編集 |
| `fn + F4` | 参照の絶対／相対切替（数式バー内） |
| `fn + F7` | スペルチェック |
| `fn + F9` | 開いているブックの全シートを再計算 |
| `fn + F11` | グラフシートを挿入 |
| `fn + F12` | 名前を付けて保存 |

---

> **Tips**
> - Mac の「システム設定 → キーボード」で「F1、F2 などのキーを標準のファンクションキーとして使用」を ON にすると、`fn` を押さずに F キーが使えるようになります。Excel を頻繁に使う方にはこの設定がおすすめです。
> - **US / JIS 配列の主な違い**: US キーボードではバッククォート `` ` `` が `1` の左（JIS では `@` + Shift）、`;` が `L` の右（JIS では Shift が必要）にあります。本資料のキー位置は US 配列基準です。
